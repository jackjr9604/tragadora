import type{Metadata}from'next';import{PublicPageShell}from'@/components/public/PublicPageShell';import{ExchangeDirectory}from'@/components/public/ExchangeDirectory';import{getPublicExchanges}from'@/lib/exchanges';import{resolvePublicLanguage}from'@/lib/language';export const metadata:Metadata={title:'Exchanges | Tradagora',description:'Plataformas para comprar, vender y operar activos digitales.'};export const revalidate=60;export default async function Page({searchParams}:{searchParams:Promise<{lang?:string|string[]}>}){const language=await resolvePublicLanguage((await searchParams).lang),items=await getPublicExchanges(language);return <PublicPageShell language={language}><section className="tg-hero"><div className="mx-auto max-w-[1440px] px-4 py-10 sm:px-6 lg:px-8"><p className="tg-eyebrow">Exchanges</p><h1 className="mt-3 text-4xl font-bold sm:text-5xl">Activos digitales, con criterio.</h1><p className="mt-3 text-slate-400">Plataformas para comprar, vender y operar activos digitales.</p></div></section><main className="mx-auto max-w-[1440px] px-4 py-8 sm:px-6 lg:px-8"><ExchangeDirectory items={items}/></main></PublicPageShell>}
