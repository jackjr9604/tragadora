export function SafeMarkdown({source}:{source:string}){const lines=source.split(/\r?\n/);return <div className="space-y-4 text-[1.05rem] leading-8 text-slate-300">{lines.map((line,index)=>{if(line.startsWith('### '))return <h3 key={index} className="pt-4 text-xl font-bold text-white">{inline(line.slice(4))}</h3>;if(line.startsWith('## '))return <h2 key={index} className="pt-6 text-2xl font-bold text-white">{inline(line.slice(3))}</h2>;if(line.startsWith('# '))return <h2 key={index} className="pt-6 text-3xl font-bold text-white">{inline(line.slice(2))}</h2>;if(line.startsWith('- '))return <div key={index} className="flex gap-3"><span className="text-amber-300">•</span><p>{inline(line.slice(2))}</p></div>;if(!line.trim())return <div key={index} className="h-2"/>;return <p key={index}>{inline(line)}</p>})}</div>}
function inline(text:string){const parts=text.split(/(\*\*[^*]+\*\*|\[[^\]]+\]\(https?:\/\/[^)]+\))/g);return parts.map((part,i)=>{const bold=part.match(/^\*\*(.+)\*\*$/);if(bold)return <strong key={i} className="text-white">{bold[1]}</strong>;const link=part.match(/^\[([^\]]+)\]\((https?:\/\/[^)]+)\)$/);if(link)return <a key={i} href={link[2]} target="_blank" rel="noopener noreferrer" className="text-amber-300 underline">{link[1]}</a>;return part})}
